export interface Globals {
  filePath: string;
  isDirty: boolean;
  zen: boolean;
  renderTree: () => void;
  inputTime: number;
  renderTime: number;
  ln: number;
  col: number;
  lnCount: number;
}
